/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'upload.wikimedia.org','img.clerk.com','res.cloudinary.com'],
      },
    
};

export default nextConfig;
